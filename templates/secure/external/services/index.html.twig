{% extends 'base.html.twig' %}

{% block title %}
    Mis Servicios
{% endblock %}

{% block body %}
    <div class="container-fluid">
        <!-- start page title -->
        <div class="row">
            <div class="col-12">
                <div class="page-title-box d-sm-flex align-items-center justify-content-between bg-galaxy-transparent">
                    <div class="page-title-right">
                        <ol class="breadcrumb m-0">
                            <li class="breadcrumb-item">
                                <a href="{{ path('app_home') }}">
                                    Mi portal
                                </a>
                            </li>
                            <li class="breadcrumb-item active">
                                Mis Servicios
                            </li>
                        </ol>
                    </div>
                </div>
            </div>
        </div>

        {% for label, messages in app.flashes %}
            {% for message in messages %}
                <div class="alert alert-{{ label }} alert-dismissible fade show mt-2" role="alert">
                    {{ message }}
                    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                </div>
            {% endfor %}
        {% endfor %}

        <div class="row">
            <div class="col-12 d-flex justify-content-between align-items-center mb-3">
                <h4 class="mb-0">Mis Servicios</h4>
                <a href="{{ path('app_secure_external_services_new') }}" class="btn btn-primary">
                    <i class="ri-add-line"></i> Nuevo Servicio
                </a>
            </div>
        </div>

        <div class="row">
            <div class="col-12">
                {% if services is empty %}
                    <div class="alert alert-info">
                        No tiene servicios registrados.
                    </div>
                {% else %}
                    <div class="card">
                        <div class="card-body">
                            <!-- Spinner de carga -->
                            <div id="loading-spinner" class="text-center py-5">
                                <div class="spinner-border text-primary" role="status" style="width: 3rem; height: 3rem;">
                                    <span class="visually-hidden">Cargando...</span>
                                </div>
                                <p class="mt-3 text-muted">Cargando servicios...</p>
                            </div>

                            <!-- Tabla oculta inicialmente -->
                            <div class="table-responsive" id="table-container" style="display: none;">
                                <table class="table table-hover" id="tabla-services">
                                    <thead>
                                        <tr>
                                            <th>ID</th>
                                            <th>Fecha</th>
                                            <th>Empresa</th>
                                            <th>Nro. Serie</th>
                                            <th>Nro. Seguimiento</th>
                                            <th>Acciones</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        {% for service in services %}
                                            <tr>
                                                <td>{{ service.serviceid }}</td>
                                                <td>{{ service.servicedate ? service.servicedate|date('d/m/Y') : '-' }}</td>
                                                <td>{{ service.serviceempresa ?? '-' }}</td>
                                                <td>{{ service.servicenroserie ?? '-' }}</td>
                                                <td>{{ service.servicenroseguimiento ?? '-' }}</td>
                                                <td class="text-center">
                                                    <div class="btn-group">
                                                        <a href="{{ path('app_secure_external_services_show', {id: service.serviceid}) }}"
                                                           class="btn btn-sm btn-secondary mx-1"
                                                           title="Ver detalle">
                                                            <i style="font-size:16px;" class="ri-eye-fill"></i>
                                                        </a>
                                                        <a href="{{ path('app_secure_external_services_edit', {id: service.serviceid}) }}"
                                                           class="btn btn-sm btn-warning mx-1"
                                                           title="Editar">
                                                            <i style="font-size:16px;" class="ri-edit-2-fill"></i>
                                                        </a>
                                                    </div>
                                                </td>
                                            </tr>
                                        {% endfor %}
                                    </tbody>
                                </table>
                            </div>
                        </div>
                    </div>
                {% endif %}
            </div>
        </div>
    </div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script>
        window.addEventListener('load', function() {
            console.log('Página cargada, inicializando DataTable External...');

            // Esperar a que jQuery esté disponible
            var checkJQuery = setInterval(function() {
                if (typeof $ !== 'undefined' && $.fn && $.fn.DataTable && $('#tabla-services').length) {
                    clearInterval(checkJQuery);
                    console.log('jQuery y DataTable disponibles');

                    var table = $('#tabla-services').DataTable({
                        "language": {
                            "decimal": "",
                            "emptyTable": "No hay datos disponibles en la tabla",
                            "info": "Mostrando _START_ a _END_ de _TOTAL_ registros",
                            "infoEmpty": "Mostrando 0 a 0 de 0 registros",
                            "infoFiltered": "(filtrado de _MAX_ registros totales)",
                            "infoPostFix": "",
                            "thousands": ",",
                            "lengthMenu": "Mostrar _MENU_ registros",
                            "loadingRecords": "Cargando...",
                            "processing": "Procesando...",
                            "search": "Buscar:",
                            "zeroRecords": "No se encontraron registros coincidentes",
                            "paginate": {
                                "first": "Primero",
                                "last": "Último",
                                "next": "Siguiente",
                                "previous": "Anterior"
                            },
                            "aria": {
                                "sortAscending": ": activar para ordenar la columna de manera ascendente",
                                "sortDescending": ": activar para ordenar la columna de manera descendente"
                            }
                        },
                        "order": [[0, "desc"]],
                        "pageLength": 10,
                        "lengthMenu": [[10, 25, 50, 100, -1], [10, 25, 50, 100, "Todos"]],
                        "columnDefs": [
                            { "orderable": false, "targets": 5 }
                        ],
                        "dom": '<"row"<"col-sm-12 col-md-6"l><"col-sm-12 col-md-6"f>>' +
                               '<"row"<"col-sm-12"tr>>' +
                               '<"row"<"col-sm-12 col-md-5"i><"col-sm-12 col-md-7"p>>',
                        "processing": true,
                        "initComplete": function(settings, json) {
                            // Ocultar spinner y mostrar tabla
                            $('#loading-spinner').hide();
                            $('#table-container').fadeIn();
                            console.log('DataTable External inicializado correctamente');
                        }
                    });
                }
            }, 100);

            // Timeout después de 5 segundos
            setTimeout(function() {
                clearInterval(checkJQuery);
                if (typeof $ === 'undefined') {
                    console.error('jQuery no se cargó después de 5 segundos');
                }
            }, 5000);
        });
    </script>
{% endblock %}
