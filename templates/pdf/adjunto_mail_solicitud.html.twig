<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <style>
        body {
            font-family: Arial, sans-serif;
            font-size: 12px;
            color: #333;
            margin: 0;
            padding: 20px;
        }

        .header {
            background-color: #e7d400;
            color: #ffffff;
            padding: 10px 15px;
            margin: 0;
        }

        .section-box {
            background-color: #f5f5f5;
            border-radius: 4px;
            padding: 15px;
            margin-bottom: 20px;
        }

        .section-label {
            background-color: #e8e8e8;
            padding: 8px 16px;
            border-radius: 4px;
            font-weight: 500;
            font-size: 14px;
            display: inline-block;
            margin-bottom: 15px;
        }

        .row {
            width: 100%;
            margin-bottom: 10px;
        }

        .label {
            font-weight: 500;
            color: #555;
        }

        .value {
            color: #000;
        }

        table {
            width: 100%;
            border-collapse: collapse;
        }

        table td {
            padding: 5px 10px;
            vertical-align: top;
        }

        .footer {
            margin-top: 30px;
            font-size: 10px;
            text-align: center;
            color: #777;
        }
        .bordered-table td {
    border: 1px solid #999;
    padding: 5px 10px;
}
.bordered-table .table-header td {
    background-color: #e8e8e8;
    font-weight: bold;
}
    </style>
</head>
<body>
    <img src="data:image/png;base64,{{ logo }}" alt="logo" style="max-width: 200px;">
    <h1>Solicitud de servicio - Centro de reparaciones</h1>
    <p>Fecha de alta: {{ solicitud.servicedate|date('d/m/Y') }}</p>
    <p>Número de seguimiento: {{ solicitud.servicenroseguimiento ?? '-'}}</p>
    <p>Tipo de servicio: {{ tipo }}</p>
    <div class="section-box">
    <table class="bordered-table">
        <tr class="table-header">
            <td colspan="4">DATOS DEL MATERIAL</td>
        </tr>
        <tr>
            <td class="label">Marca</td>
            <td class="value">{{ marca }}</td>
            <td class="label">Código de Catálogo</td>
            <td class="value">{{ solicitud.servicecodcatalogo }}</td>
        </tr>
        <tr>
            <td class="label">Serie</td>
            <td class="value">{{ solicitud.servicenroserie }}</td>
            <td class="label">Descripción Falla</td>
            <td class="value">{{ solicitud.servicefalla }}</td>
        </tr>
    </table>
</div>
<div class="section-box">
    <table class="bordered-table">
        <tr class="table-header">
            <td colspan="4">DATOS DEL CLIENTE</td>
        </tr>
        <tr>
            <td class="label">Empresa</td>
            <td class="value">{{ solicitud.serviceempresa ?? '-' }}</td>
            <td class="label">C.U.I.T.</td>
            <td class="value">{{ solicitud.servicecuit ?? '-' }}</td>
        </tr>
        <tr>
            <td class="label">Nombre Contacto</td>
            <td class="value">{{ solicitud.servicecontacto ?? '-'}}</td>
            <td class="label">Email</td>
            <td class="value">{{ solicitud.serviceemail ?? '-'}}</td>
        </tr>
        <tr>
            <td class="label">Domicilio Entrega</td>
            <td class="value">{{ solicitud.servicedireccion ?? '-'}}</td>
            <td class="label">Cód. Postal</td>
            <td class="value">{{ solicitud.servicecodpostal ?? '-' }}</td>
        </tr>
        <tr>
            <td class="label">Teléfono</td>
            <td class="value">{{ solicitud.servicetelefono ?? '-' }}</td>
            <td class="label">País</td>
            <td class="value">{{ pais ?? '-'}}</td>
        </tr>
    </table>
</div>
    <div class="footer">
        <p>Racklatina S.A.</p>
    </div>
</body>
</html>