<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <style>
        body {
            font-family: Arial, sans-serif;
            font-size: 12px;
            color: #333;
            margin: 0;
            padding: 20px;
        }

        .header {
            background-color: #e7d400;
            color: #ffffff;
            padding: 10px 15px;
            margin: 0;
        }

        .section-box {
            background-color: #f5f5f5;
            border-radius: 4px;
            padding: 15px;
            margin-bottom: 20px;
        }

        .section-label {
            background-color: #e8e8e8;
            padding: 8px 16px;
            border-radius: 4px;
            font-weight: 500;
            font-size: 14px;
            display: inline-block;
            margin-bottom: 15px;
        }

        .row {
            width: 100%;
            margin-bottom: 10px;
        }

        .label {
            font-weight: 500;
            color: #555;
        }

        .value {
            color: #000;
        }

        table {
            width: 100%;
            border-collapse: collapse;
        }

        table td {
            padding: 5px 10px;
            vertical-align: top;
        }

        .footer {
            margin-top: 30px;
            font-size: 10px;
            text-align: center;
            color: #777;
        }
    </style>
</head>
<body>
    <h1 class="header">FORMULARIO DE SERVICIO - SEGUIMIENTO</h1>
    <h1 class="header">Nro. {{ numero_seguimiento }} - LABORATORIO RACKLATINA</h1>

    <div class="section-box">
        <div class="section-label">Datos Principales</div>
        <table>
            <tr>
                <td width="50%">
                    <p><span class="label">País:</span> <span class="value">{{ solicitud.pais }}</span></p>
                    <p><span class="label">Provincia:</span> <span class="value">{{ solicitud.provincia }}</span></p>
                    <p><span class="label">Localidad:</span> <span class="value">{{ solicitud.localidad }}</span></p>
                    <p><span class="label">Empresa:</span> <span class="value">{{ solicitud.empresa }}</span></p>
                </td>
                <td width="50%">
                    <p><span class="label">Contacto:</span> <span class="value">{{ solicitud.contacto }}</span></p>
                    <p><span class="label">Email:</span> <span class="value">{{ solicitud.email }}</span></p>
                    <p><span class="label">Dirección:</span> <span class="value">{{ solicitud.direccion }}</span></p>
                    <p><span class="label">Transporte:</span> <span class="value">{{ solicitud.transporteNombre }}</span></p>
                </td>
            </tr>
        </table>
    </div>

    <div class="section-box">
        <div class="section-label">Información del Material</div>
        <table>
            <tr>
                <td width="50%">
                    <p><span class="label">Marca:</span> <span class="value">{{ solicitud.marca }}</span></p>
                    <p><span class="label">Código Catálogo:</span> <span class="value">{{ solicitud.codCatalogo }}</span></p>
                    <p><span class="label">Número de Serie:</span> <span class="value">{{ solicitud.nroSerie }}</span></p>
                </td>
                <td width="50%">
                    <p><span class="label">Descripción de la Falla:</span></p>
                    <p class="value">{{ solicitud.falla }}</p>
                    <p><span class="label">¿Adquirido en los últimos 12 meses?:</span> <span class="value">{{ solicitud.adquiridoUltimos12Meses ? 'Sí' : 'No' }}</span></p>
                </td>
            </tr>
        </table>
    </div>

    <div class="footer">
        <p>Racklatina S.A. © {{ "now"|date("Y") }}. Todos los derechos reservados.</p>
    </div>
</body>
</html>