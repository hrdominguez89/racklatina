{% extends 'base_public.html.twig' %}

{% block title %}
    {{ title }}
{% endblock %}

{% block body %}
    <div class="container-fluid">
        <div class="row justify-content-center">
            <div class="col-lg-8 col-md-10">
                <div class="success-container text-center">
                    <div class="success-icon mb-4">
                        <i class="ri-checkbox-circle-line"></i>
                    </div>

                    <div class="success-message-box mb-4 p-4">
                        <h1 class="success-title mb-3">¡Solicitud Enviada Exitosamente!</h1>
                    </div>

                    <div class="alert alert-success shadow-sm" role="alert">
                        <h4 class="alert-heading">Su número de seguimiento es:</h4>
                        <p class="display-4 mb-0 fw-bold text-success">{{ nroSeguimiento }}</p>
                    </div>

                    <div class="info-box mt-4 p-4">
                        <p class="mb-3">
                            <i class="ri-mail-line me-2"></i>
                            Hemos enviado un correo electrónico de confirmación con los detalles de su solicitud.
                        </p>
                        <p class="mb-3">
                            <i class="ri-information-line me-2"></i>
                            Guarde su número de seguimiento para futuras consultas sobre el estado de su servicio.
                        </p>
                        <p class="mb-0">
                            <i class="ri-customer-service-2-line me-2"></i>
                            Nuestro equipo se pondrá en contacto con usted a la brevedad.
                        </p>
                    </div>

                    <div class="mt-5">
                        <a href="{{ path('app_public_services_new') }}" class="btn btn-primary btn-lg me-2">
                            <i class="ri-add-line me-1"></i>
                            Nueva Solicitud
                        </a>
                        <a href="{{ path('app_home') }}" class="btn btn-secondary btn-lg">
                            <i class="ri-home-line me-1"></i>
                            Volver al Inicio
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <style>
        .success-container {
            padding: 40px 20px;
            margin-top: 40px;
        }

        .success-icon {
            font-size: 120px;
            color: #28a745;
            animation: scaleIn 0.5s ease-in-out;
        }

        @keyframes scaleIn {
            0% {
                transform: scale(0);
            }
            50% {
                transform: scale(1.1);
            }
            100% {
                transform: scale(1);
            }
        }

        .success-title {
            color: #333;
            font-weight: 600;
        }

        .success-message-box {
            background-color: #d4edda;
            border-radius: 8px;
            border: 2px solid #28a745;
        }

        .success-message-box .success-title {
            color: #155724;
            margin-bottom: 0;
        }

        .info-box {
            background-color: #f8f9fa;
            border-radius: 8px;
            border: 1px solid #e0e0e0;
        }

        .info-box p {
            font-size: 1rem;
            color: #555;
        }

        .info-box i {
            color: #007bff;
            font-size: 1.2rem;
        }

        .alert-success {
            border-radius: 8px;
            border: 2px solid #28a745;
        }

        .btn-lg {
            padding: 12px 30px;
            font-size: 1.1rem;
        }
    </style>
{% endblock %}
